pipeline {
    agent {
    node {
        label 'zero'
        customWorkspace '/mnt/homeworkWS'
    }
}
      stages {
        stage('static web') { 
            steps {
                echo "Hey...This is Dev. environment"
            }
        }
        
    }
}
